let firstname = 'ozgur';
let surname = 'altinok';

let fullName = firstname + surname;

let nameLength = fullName.length;

let upperCase = fullName.toUpperCase()
let lowerCase = fullName.toLowerCase()

let index = fullName.indexOf('o')
let lastIndex = fullName.indexOf('o')

let slice = fullName.slice(0,1)
let repl = fullName.replace(' ', '')
let replAll = fullName.replaceAll(' ', '')